# macOS Finder metadata — never belongs in the repo
.DS_Store
**/.DS_Store

# Editor / OS junk
*.swp
Thumbs.db

# Dependencies (installed locally, not committed)
node_modules/
# Firebase service-account key for one-off local scripts — NEVER commit
service-account.json
